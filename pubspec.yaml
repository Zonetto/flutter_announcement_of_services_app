name: announcement_of_services
description: A new Flutter project.
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=3.1.2 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  intl: null
  flutter_localizations:
    sdk: flutter  

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/images/clients_home_appliance.jpg
    - assets/images/clients_home_appliance1.png
flutter_intl:
  enabled: true
